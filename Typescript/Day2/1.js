var countstringlen = function (array1) {
    array1.forEach(function (element) {
        console.log(element, " ", element.length);
    });
};
var arr = ["aditi", "jain", "mradul", "utkarsh"];
countstringlen(arr);
